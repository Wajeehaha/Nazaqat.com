# Environment Variables for Vercel Deployment
# Copy these to your Vercel dashboard: Settings > Environment Variables

# Required Variables
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/nazakat-store?retryWrites=true&w=majority
JWT_SECRET=nazakat-super-secure-jwt-secret-key-2025-minimum-32-characters
NODE_ENV=production
FRONTEND_URL=https://nazakat-nail-store.web.app

# Optional: PayFast Payment Gateway
PAYFAST_MERCHANT_ID=your_merchant_id
PAYFAST_MERCHANT_KEY=your_merchant_key
PAYFAST_PASSPHRASE=your_passphrase

# Optional: Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Note: Replace the placeholder values with your actual credentials
# Never commit real credentials to version control
